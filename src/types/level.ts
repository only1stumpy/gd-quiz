export type LevelData = {
  id: number;
  ingame_id: number;
  placement: number;
  name: string;
  points: number;
  list_percent: number;
  length: number;
  holder: string;
  verification_url: string;
  date_created: string;
}